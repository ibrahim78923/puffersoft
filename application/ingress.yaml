apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: ingress-1
  namespace: frontend

spec:
  rules:
    - host: www.dreggystore.com
      http:
        paths:
          - path: /
            backend:
              serviceName: websvc
              servicePort: 80
